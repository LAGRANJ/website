<%= form_for([:admin, garant]) do |f| %>
  <% if garant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(garant.errors.count, "error") %> prohibited this garant from being saved:</h2>

      <ul>
      <% garant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">    
    <%= f.label :name, "Название" %>
    <%= f.text_field :name, class:"form-control"%>
  </div>
  <div class="form-group">    
    <%= f.label :description, "Описание" %>
    <%= f.text_area :description, class:"form-control", rows:25 %>
  </div>
  <div class="form-group">
    <%= f.label :cost, "Стоимость" %>
    <%= f.text_field :cost, class:"form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :region, "Стоимость" %>
    <%= f.text_field :region, class:"form-control" %>
  </div>
  <div class="actions">
    <%= f.submit "Сохранить"%>
  </div>
<% end %>
